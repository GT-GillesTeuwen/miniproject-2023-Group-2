import { appGoogleRegisterFeature } from './app-google-register-feature';

describe('appGoogleRegisterFeature', () => {
  it('should work', () => {
    expect(appGoogleRegisterFeature()).toEqual('app-google-register-feature');
  });
});
