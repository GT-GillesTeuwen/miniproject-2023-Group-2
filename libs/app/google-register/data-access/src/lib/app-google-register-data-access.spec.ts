import { appGoogleRegisterDataAccess } from './app-google-register-data-access';

describe('appGoogleRegisterDataAccess', () => {
  it('should work', () => {
    expect(appGoogleRegisterDataAccess()).toEqual(
      'app-google-register-data-access'
    );
  });
});
