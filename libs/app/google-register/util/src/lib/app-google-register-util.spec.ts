import { appGoogleRegisterUtil } from './app-google-register-util';

describe('appGoogleRegisterUtil', () => {
  it('should work', () => {
    expect(appGoogleRegisterUtil()).toEqual('app-google-register-util');
  });
});
